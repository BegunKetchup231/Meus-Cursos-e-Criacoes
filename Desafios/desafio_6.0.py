from math import sqrt

num = int(input('Digite um número: '))
raiz = sqrt(num)
print(f'A raiz de {num:.2f} é {raiz:.2f}')

#Control + Space < Abre as bibliotecas

import emoji
print(emoji.emojize(":smile:", language='alias'))